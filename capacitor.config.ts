import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.primoveneroso.randomsexposition',
  appName: 'Random Position',
  webDir: 'dist'
};

export default config;
